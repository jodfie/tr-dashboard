# Minimal — direct port access, no reverse proxy.
# You must route /api, /audio, /health to tr-engine yourself.
services:
  tr-dashboard:
    image: ghcr.io/trunk-reporter/tr-dashboard:latest
    # build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
